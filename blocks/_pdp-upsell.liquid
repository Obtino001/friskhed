{%- liquid
  assign current_product = closest.product | default: product
-%}
{% render 'cart-upsell', exclude_product: current_product, context: 'pdp' %}

{% schema %}
{
  "name": "PDP complete order",
  "tag": null,
  "settings": [
    {
      "type": "paragraph",
      "content": "On scents: classic + color applicator (pick one). On applicator PDPs: mix-pack is hidden, only scents are suggested."
    }
  ],
  "presets": [
    {
      "name": "PDP complete order"
    }
  ]
}
{% endschema %}
